{
  "name": "@tms/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build --workspace @tms/shared && npm run build && node dist/service.test.js && node dist/priority1.test.js",
    "verify:start": "npm run build && node scripts/verify-start.mjs"
  },
  "dependencies": {
    "@tms/shared": "file:../../packages/shared",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "jose": "^6.2.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.1",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
